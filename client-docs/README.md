Company documents (profile PDF, price lists, contact details) live here on the local machine only.
They are excluded from git by `.gitignore` because this repository is public and the price lists contain wholesale pricing.
